<!-- 以下是对自定义组件的引用 -->
<component-tag-name list="{{list}}">
  <view class="list-item" wx:for="{{list}}">
    <navigator url="../detail/detail">
      {{index + 1}}.{{item}}
    </navigator>
  </view>
</component-tag-name>